// let productPrice = 10;
// let ammountInWallet = 5;
// let isProductAvailable = true;
// let useCreditAvailable = true;

// //Logical AND (&&)
// //if (ammountInWallet >= productPrice && isProductAvailable === true) { 
//     //console.log('You can purchase it') };

// // Logical OR (||)
// //if (ammountInWallet >=productPrice || useCreditAvailable === true) { 
// //    console.log('You can purchase this product') }

// //Using Logical NOT (!) to invert the value
let isUserLoggedIn = false;
let isUserLoggedOut = !isUserLoggedIn;
console.log(isUserLoggedOut);

// let age = 15;
// //using the ternary (?) operator to assign a value to a variable
// let isAdult = age >= 18 ? true : false;

// console.log(isAdult);